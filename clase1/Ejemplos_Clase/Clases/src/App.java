import java.util.Scanner;

public class App {
    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);
        
        Short corto1 = new Short(); //esto es instanciar la clase: sacar un objeto

        //Ejercicios:

        //Instanciar dos objetos de la clase Short usando el constructor por defecto y el constructor con parámetros

        //Instanciar dos objetos de la clase Short usando el constructor por defecto y el constructor con parámetros

        //Escribir por consola los atributos de los objetos. Tip: Debes acceder a los atributos usando métodos getters
        //ejemplo: System.out.PrintLn( miVuelo.getOrigen() );
    }
}
